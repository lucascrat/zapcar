import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'br.com.client.chegoja',
    appName: 'chegoja',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
